import 'package:flutter/material.dart';

class AppRadius {
  static const sm = BorderRadius.all(Radius.circular(4));
  static const md = BorderRadius.all(Radius.circular(8));
  static const lg = BorderRadius.all(Radius.circular(16));
  static const xl = BorderRadius.all(Radius.circular(24));
  static const full = BorderRadius.all(Radius.circular(100));

  static const double small = 4;
  static const double medium = 8;
  static const double lager = 16;
  static const double extraLager = 24;
  static const double fullRadius = 100;
}
